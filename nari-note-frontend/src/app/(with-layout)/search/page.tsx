export const runtime = 'edge';

import { UnifiedSearchPage as UnifiedSearchPageComponent } from '@/features/search/pages';

export default function UnifiedSearchPage() {
  return <UnifiedSearchPageComponent />;
}
